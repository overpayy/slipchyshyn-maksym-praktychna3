#include <iostream>
using namespace std;

int gcd(int a, int b) {
    while (b != 0) {
        int r = a % b;
        a = b;
        b = r;
    }
    return a;
}

int main() {
    int x, y;
    cout << "Enter two integers: ";
    cin >> x >> y;

    int result = gcd(x, y);

    cout << "The greatest common divisor of " << x << " and " << y << " is " << result << endl;

    return 0;
}
